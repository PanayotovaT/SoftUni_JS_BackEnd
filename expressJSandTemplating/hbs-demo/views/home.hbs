    <h1>Hello, handebars!</h1>
    {{#with user}}
    <div>
        <p>Welcome, {{name}}!</p>
        <p>Your email is {{email}}!</p>
    </div>
    {{/with}}
    {{#if count}}
    <p>Site visited {{count}} times</p>
    {{else}}
    <p>You are the first visitor.</p>
    {{/if}}
